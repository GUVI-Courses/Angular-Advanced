import { ReplaceWordPipe } from './replace-word.pipe';

describe('ReplaceWordPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceWordPipe();
    expect(pipe).toBeTruthy();
  });
});
